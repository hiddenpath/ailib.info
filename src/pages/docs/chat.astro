---
layout: '../../layouts/DocsLayout.astro'
title: 'Chat & Streaming'
---
<h1>Chat & Streaming</h1>
<p>High-level helper surfaces a consistent interface across providers.</p>
<h2>Basic Chat</h2>
<pre><code>const res = await client.chat({
  model: 'gpt-4o',
  messages: [
    { role: 'user', content: 'Explain transformers.' }
  ]
});
console.log(res.text);
</code></pre>
<h2>Streaming</h2>
<pre><code>for await (const delta of client.stream({
  model: 'claude-3-haiku',
  messages,
  tools
})) {
  if (delta.type === 'text') process.stdout.write(delta.text);
  if (delta.type === 'tool_call') {/* handle */}
}
</code></pre>
<p>All providers normalized to the same delta events.</p>
