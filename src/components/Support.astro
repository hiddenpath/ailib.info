---
const { locale='en' } = Astro.props;
const isZh = locale==='zh';
---

<section id="contact">
  <h2>{isZh ? '商业与支持' : 'Commercial & Support'}</h2>
  <p class="dim" style="max-width:760px;">
    {isZh
      ? <>当前阶段：征集早期使用反馈与企业级需求。我们计划提供分层支持：集成指导、模型策略咨询、性能基准验证、架构评审等。填写下列表单即可加入。</>
      : <>We are gathering early adopter feedback & enterprise requirements. Planned tiers include integration guidance, model strategy consulting, performance validation, and architecture reviews. Use the form below to express interest.</>}
  </p>
  <form method="POST" action="https://formspree.io/f/your-form-id" style="margin-top:30px;">
    <div class="row">
      <div style="flex:1; min-width:240px;">
        <label>{isZh ? '姓名' : 'Name'}</label>
        <input required name="name" placeholder={isZh ? '张三' : 'Jane Doe'} />
      </div>
      <div style="flex:1; min-width:240px;">
        <label>Email</label>
        <input required type="email" name="email" placeholder="you@company.com" />
      </div>
    </div>
    <div class="row" style="margin-top:16px;">
      <div style="flex:1; min-width:240px;">
        <label>{isZh ? '公司 / 组织' : 'Company / Org'}</label>
        <input name="company" placeholder="Acme Corp" />
      </div>
      <div style="flex:1; min-width:240px;">
        <label>{isZh ? '预计用例' : 'Intended Use Case'}</label>
        <input name="use_case" placeholder={isZh ? '多提供商路由 / 成本优化' : 'Multi-provider routing / cost optimization'} />
      </div>
    </div>
    <div style="margin-top:16px;">
      <label>{isZh ? '需求描述 / 时间表' : 'Need Description / Timeline'}</label>
      <textarea name="details" rows="5" placeholder={isZh ? 'Q4 计划上线，需可靠性策略...' : 'Planning production Q4, need reliability strategy...'}></textarea>
    </div>
    <div style="margin-top:18px;">
      <button class="button" type="submit">{isZh ? '提交' : 'Submit'}</button>
      <span class="notice">
        {isZh
          ? '我们通常在 2–3 个工作日内回复。不会向第三方共享信息。'
          : 'We usually reply within 2–3 business days. Information is not shared with third parties.'}
      </span>
    </div>
  </form>
</section>