---
const { locale = 'en' } = Astro.props;
const isZh = locale === 'zh';
---

<section style="padding-top:120px;">
  <div style="position:relative;">
    <span class="badge">{isZh ? '早期预览' : 'Early Preview'}</span>
    <h1 style="margin:18px 0 20px; font-size:48px; max-width:820px;">
      {
        isZh ? (
          <>Ai-lib: 一个API客户端连通所有AI模型 (Rust)</>
        ) : (
          <>Ai-lib: Unified Multi-Provider AI API Client for Rust</>
        )
      }
    </h1>
    <p style="font-size:20px; max-width:860px; color:var(--text-dim); margin:0 0 28px;">
      {
        isZh ? (
          <>
            一个API客户端，多个提供商：可靠流式处理、函数调用、推理大模型与可观测性 — SDK开销&lt;1ms。
          </>
        ) : (
          <>
            One API client, multiple providers: reliable streaming, function calls,
            reasoning models, and observability — with &lt;1ms overhead.
          </>
        )
      }
    </p>
    <div class="btn-row">
      <a class="button" href="https://github.com/hiddenpath/ai-lib" target="_blank" rel="noopener">
        ★ GitHub
      </a>
      <a class="button secondary" href="#quickstart">
        {isZh ? '快速开始' : 'Quick Start'}
      </a>
      <a class="button outline" href="#contact">
        {isZh ? '商业支持' : 'Enterprise & Support'}
      </a>
    </div>
    <p style="margin-top:26px; font-size:13px; color:var(--text-dim);">
      {
        isZh ? (
          <>双许可证 MIT / Apache-2.0 · 专注生产级抽象 · 无供应商锁定</>
        ) : (
          <>Dual-licensed MIT / Apache-2.0 · Production-grade abstractions · Vendor neutral</>
        )
      }
    </p>
  </div>
</section>